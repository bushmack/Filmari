<Window x:Class="filamri.WatchPartyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="🎬 Совместный просмотр" 
        Height="700" 
        Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A2E"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="NeonButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Левая часть: видео -->
        <Border Grid.Column="0" Background="#0F0F1F" Margin="5" CornerRadius="15">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <Border Background="#2A2A3E" CornerRadius="15" Padding="10,5">
                        <TextBlock x:Name="RoomInfoText" FontSize="12" Foreground="#A29BFE" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Background="#2A2A3E" CornerRadius="15" Padding="10,5" Margin="0,5,0,0">
                        <TextBlock x:Name="RoomIdText" FontSize="10" Foreground="#888888" HorizontalAlignment="Center"/>
                    </Border>
                    <Border Background="#2A2A3E" CornerRadius="15" Padding="10,5" Margin="0,5,0,0">
                        <TextBlock x:Name="PartnerInfoText" FontSize="12" Foreground="#A29BFE" HorizontalAlignment="Center"/>
                    </Border>
                </StackPanel>

                <!-- WebView2 для встраивания видео -->
                <Border Grid.Row="1" Background="#2A2A3E" CornerRadius="20" BorderBrush="#6C5CE7" BorderThickness="2">
                    <Grid>
                        <wv2:WebView2 x:Name="VideoWebView" />
                        <Viewbox x:Name="PlayIcon" Visibility="Visible" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="▶" FontSize="80" Foreground="#6C5CE7" Opacity="0.9"/>
                        </Viewbox>
                        <TextBlock x:Name="LoadingText" Text="⏳ Ожидание партнера..." 
                                   FontSize="20" Foreground="#888888" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <!-- Управление -->
                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="PlayPauseButton" Content="⏸" Width="60" Height="60" 
                                FontSize="28" Background="#6C5CE7" Click="PlayPauseButton_Click"
                                Style="{StaticResource NeonButton}" Margin="5"/>
                        <Button x:Name="ExitButton" Content="🚪 Выйти" Width="100" Height="60" 
                                FontSize="16" Background="#FF6C5CE7" Click="ExitButton_Click"
                                Style="{StaticResource NeonButton}" Margin="5"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Правая часть: чат -->
        <Border Grid.Column="1" Background="#2A2A3E" Margin="5" CornerRadius="15">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="DebugConsole" 
         Grid.Row="3" 
         Height="100" 
         Background="#1A1A2E" 
         Foreground="#00FF00" 
         FontFamily="Consolas" 
         FontSize="10" 
         VerticalScrollBarVisibility="Auto"
         IsReadOnly="True"/>

                <TextBlock Text="💬 ЧАТ" FontSize="18" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1" x:Name="MessagesScrollViewer" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="MessagesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,5" Padding="8" CornerRadius="10" Background="#1A1A2E">
                                    <StackPanel>
                                        <TextBlock Text="{Binding UserName}" FontWeight="Bold" Foreground="#6C5CE7" FontSize="12"/>
                                        <TextBlock Text="{Binding Text}" Foreground="White" TextWrapping="Wrap" FontSize="13"/>
                                        <TextBlock Text="{Binding Time}" FontSize="9" Foreground="#888888" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="MessageTextBox" 
                             Grid.Column="0"
                             Height="35"
                             Padding="10"
                             Background="#1A1A2E"
                             Foreground="White"
                             BorderBrush="#6C5CE7"
                             BorderThickness="1"
                             KeyDown="MessageTextBox_KeyDown"/>
                    
                    
                    <Button x:Name="SendButton" 
                            Grid.Column="1"
                            Content="📨" 
                            Width="45" 
                            Height="35"
                            Margin="5,0,0,0"
                            Background="#6C5CE7"
                            Click="SendButton_Click"
                            Style="{StaticResource NeonButton}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>